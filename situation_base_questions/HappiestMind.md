## Suppose we need to create endpoint for `search`.how would u design endpoint/api in which we need to fetch user-info, user-department from MySQL database (2 records form 2 diff tables) and users-salary-collection (1 record from mongodb collection).

